/**
 * @ Vendors
 * Include your vendor modules below.
 */
import $ from 'jquery';                             // jQuery
import Bootstrap from 'bootstrap';                  // Bootstrap
import Popper from 'popper.js';                     // Popper (Require for Bootstrap)

/**
 * @ Init JS // Custom
 * Include your custom Javascripts below. each componenats should have it's own seprate JS file.
 */

import App from './js/app';

/**
 * SCSS Loader Compile
 */
import './style.scss';
